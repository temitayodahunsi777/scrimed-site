# p.34 Vercel Preview Operator Packet

Status: `OPERATOR_ACTION_REQUIRED`; no deployment performed.

Connected read-only evidence on 2026-08-21:

- project `scrimed-site` (`prj_94JBnKm2BsZ7qHtEDbUvWmiDWLjn`);
- Node `24.x`;
- latest deployment `READY`, target `null`, project `live: false`;
- production alias promotion was not requested.

Owner: authorized Vercel release operator.

## Prerequisites And Action

1. Use the final committed gap-closure SHA and regenerated evidence fingerprints.
2. Confirm synthetic/no-PHI mode, protected-write denial, no provider credentials, no production connector flags, and no migration.
3. Create a preview only through the repository’s normal candidate-bound workflow.
4. Verify build info, health, readiness, release fingerprint, p.34 page, Product Console, read-only APIs, seven protected write denials, desktop/mobile layout, accessibility, logs, and prohibited boundaries.
5. Retain the preview deployment ID, exact commit, environment, smoke evidence, timestamp, expiry, and rollback owner.

Rollback: delete or abandon the preview deployment; do not move aliases. Completion condition: exact-candidate preview verification passes. It does not authorize merge, production promotion, investor distribution, or customer access.
